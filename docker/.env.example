# SQL Server Configuration
SA_PASSWORD=YourStrong@Password123

# RabbitMQ Configuration
RABBITMQ_USER=admin
RABBITMQ_PASS=admin123

# Redis Configuration
REDIS_PASSWORD=redis123

# MinIO Configuration
# Access Key
MINIO_ROOT_USER=minioadmin
# Secret Key (Must be strong in production)
MINIO_ROOT_PASSWORD=minioadmin123
# Console Port (Browser access)
MINIO_CONSOLE_PORT=9001
# API Port (S3 API)
MINIO_API_PORT=9000

# Backend API Configuration
# Port mapped to host
API_PORT=5500
# JWT Secret Key (Must be at least 32 chars)
JWT_SECRET_KEY=TroLiKOC_SuperSecretKey_2026_MinLength32Chars!
JWT_ISSUER=TroLiKOC
JWT_AUDIENCE=TroLiKOC

# Frontend Configuration
# Port mapped to host
FRONTEND_PORT=3000

# Cloudflare Tunnel (Optional - for exposing to internet)
CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoi...

# AI Worker Configuration
# Enable/Disable GPU (set to 'cpu' if no NVIDIA GPU)
AI_WORKER_DEVICE=cuda
